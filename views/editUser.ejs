<%- include('partials/header') %>

    <div class="ui main text container segment">
        <div class="ui huge header">Edit Blog</div>
        <form class="ui form" action="/account/<%= currentUser._id %>?_method=PUT" method="POST">
            <div class="field">
                <input type="text" name="user[firstname]" value="<%= currentUser.firstname %>" required>
            </div>
            <div class="field">
                <input type="text" name="user[lastname]" value="<%= currentUser.lastname %>" required>
            </div>
            <input class="ui black basic button" type="submit" value="Edit Account">
        </form>
    </div>

    <%- include('partials/footer') %>